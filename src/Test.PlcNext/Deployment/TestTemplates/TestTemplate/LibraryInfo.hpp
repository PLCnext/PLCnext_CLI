#include "Arp/Base/Core/Arp.hpp"

namespace $(namespace.format.cppFullName)
{

	const ArpVersion $(name.format.lastNamespacePart.format.escapeProjectName)LibraryVersion = ArpVersion(1); // adjust your library version number here

} // end of namespace $(namespace)
